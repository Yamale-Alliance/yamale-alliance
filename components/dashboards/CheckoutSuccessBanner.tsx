"use client";

import { SubscriptionCheckoutConfirm } from "@/components/checkout/SubscriptionCheckoutConfirm";

/** @deprecated Use SubscriptionCheckoutConfirm — kept for dashboard return URLs. */
export function CheckoutSuccessBanner() {
  return <SubscriptionCheckoutConfirm />;
}
